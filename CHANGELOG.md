# Changelog

本项目遵循语义化版本思路记录变化。

## [1.1.5] - 2026-08-22

### Changed

- 恢复“颗粒方案预设选择”：用户可指定颗粒预设，也可使用自动模式。
- 新建 `references/presets-grain.md`，将颗粒库收束为自然、适中、可与真实场景融合的克制颗粒语言。
- 明确“大颗粒”的“大”主要指**颗粒处理区域成片、连续、面积明显**，不再要求单颗粒巨大；单颗粒默认保持中等视觉尺度。
- 明确排除大方块、大像素、拼豆、巨大马赛克砖、巨型几何块、玩具感与糖果感颗粒。
- 颗粒选型顺序调整为：先完成背景语义分区 → 评估颗粒候选区 → 选最适合颗粒化的区域 → 再选择颗粒预设与配色。
- 颗粒区域选择由“面积优先”改为“材质适配、主体安全、自然融合优先”，通常约占整图 1/4–1/2，不为面积强行跨主体扩张。
- 方案 1–3 的自动配色收束为简洁、克制、耐看的方向，通常控制在 2–4 个核心颜色角色，以中低饱和、中性色和少量强调色为主。
- 允许局部保留细节色、轻微印刷或材质变化，但禁止为了设计感刻意堆装饰、霓虹、彩虹色或复杂纹理。
- 中英文 README 恢复颗粒方案输入与完整预设列表，并同步新的自然颗粒、克制配色与仓库结构说明。
- 同步更新 `SKILL.md`、`references/00-style-core.md`、`references/01-scene-analysis.md`、`references/02-transformation.md`、`references/03-art-direction.md`、`references/04-generation-compiler.md`、`references/05-quality-control.md`、`references/presets-grain.md`、`agents/openai.yaml`、`README.md` 与 `README.en.md`。

## [1.1.4] - 2026-08-21

### Fixed

- 统一中英文 README 的预设分类标题格式：配色、文本、比例 / 载体三类并列分类均不再使用分类序号。
- 移除 `presets-color.md`、`presets-text.md`、`presets-aspect.md` 中遗留的 8 / 9 / 10 历史章节编号，并移除配色分类中的 8.1–8.13 前缀，使三个预设库的层级格式一致。
- 保留具有实际顺序意义的编号，例如作品档案 01–05、四方案编号、执行步骤、质量控制梯度与流程节点。

## [1.1.3] - 2026-08-21

### Fixed

- 全量复核 Skill 主流程与 `references/00–05` 的调用关系，明确 `00 → 01 → 02 → 03 → 04 → 图像生成 → 05` 为唯一主执行链。
- 修正 `00-style-core.md` 中 Source Role 的序号顺序，由原来的 S2 / S3 / S4 / S1 统一为 S1 / S2 / S3 / S4，语义保持不变。
- 明确 `01-scene-analysis.md` 只输出场景分析、候选颗粒区和比例约束，不直接从比例预设库作最终选择。
- 明确 `02-transformation.md` 只输出保留 / 转译边界，并新增 `transformation_constraints` 下游接口。
- 明确 `03-art-direction.md` 为配色、文本、比例三个预设库的唯一视觉路由入口，并新增标准 `art_direction` 下游接口。
- 明确 `04-generation-compiler.md` 只消费上游决定并编译，不重新调用预设库、不重新设计方案。
- 为 `05-quality-control.md` 增加 `repair_target` 责任节点规则，使失败项按 01 / 02 / 03 / 04 定点返回，避免无条件重跑整条链。
- 保留现有全局预设库编号：8 = 配色、9 = 文本、10 = 比例 / 载体；`8.1–8.13` 连续无缺号。

## [1.1.2] - 2026-08-21

### Changed

- 移除用户输入中的“大颗粒重构方案”选择项，不再要求用户指定、映射或选择具体颗粒预设。
- 移除 `references/presets-grain.md` 及其当前执行链引用，恢复为直接依据画面内容确定核心大颗粒区域并进行明显的大颗粒重构。
- 大颗粒继续保持“区域大、颗粒大、连续完整、第一眼可见”的核心要求，不再建立独立的颗粒预设选型流程。
- 配色、文本、比例预设以及其他非相关功能保持不变。
- 中英文 README、Skill 主规则、Art Direction、Generation Compiler、Quality Control 与 `agents/openai.yaml` 同步上述逻辑。

## [1.1.1] - 2026-08-21

### Added

- 将大颗粒重构预设扩展为 15 大类、272 个具体方案，完整覆盖印刷 / 油墨、像素 / 数字方块、梦幻 / 半透明、拼豆 / 手工、马赛克 / 瓷砖、纸张 / 拼贴、绘画 / 颜料、自然物质、纺织 / 毛绒、光学 / 圆形、几何、破损 / 衰败、食物 / 糖果、金属 / 工业与特殊艺术化颗粒。
- 自动选型调整为“先判定颗粒类别，再选择具体预设”，并优先让四个方案跨类别拉开颗粒语言差异。

### Changed

- README 顶部介绍短句改为更具作品气质的文艺表达。
- README 作品档案改用 GitHub 原生 Markdown 相对路径图片表格，确保上传到 GitHub 后可直接渲染，不依赖本地阅读器对 HTML 标签的支持。
- README 的大颗粒预设列表与 `references/03-art-direction.md` 同步为完整预设库。
- SKILL.md 同步更新完整大颗粒库的自动选择逻辑。

## [1.1.0] - 2026-08-21

### Added

- 新增“大颗粒重构方案选择”：用户可直接指定一种或多种颗粒语言，也可使用自动匹配。
- 新增 24 种核心大颗粒预设：印刷缺失、缺墨斑块、粗网点、超大半调、大像素、大方块马赛克、梦幻方块、磨砂方块、玻璃方块、拼豆、串珠、亮片、碎瓷马赛克、彩窗玻璃、撕纸碎块、碎照片、厚涂颜料块、点彩大颗粒、粗砂、砾石、毛毡球、气泡、Voronoi 晶格、低多边形。
- 新增大颗粒预设的场景适配、自动选型、提示词编译与质检规则。
- 新增 `assets/showcase/` 作品档案示例图。
- 新增同一对话前两次生成后的轻量公开分享署名提示规则。

### Changed

- README 重写为面向发布后的项目主页：加入项目介绍、颗粒分域理念、作品档案、安装指南、使用指南、完整预设列表、仓库结构、作者与 License。
- 移除 README 中“发布前检查”板块。
- 可选用户输入增加大颗粒预设、配色预设、文本预设、比例 / 载体与“文字：不添加”控制项。
- 明确：若希望重构图完全无文字，需要在上传图片时说明；否则未提供文本时可由 Skill 按方案自动创作。
- 更新中文 License 说明中的展示图片说明。

## [1.0.3] - 2026-08-21

### Changed

- Skill 正式更名为 **颗粒分域 · Grain Fields**。
- 调用名统一为 `grain-fields`，仓库目录、README、Skill 元数据、License 名称与调用示例同步更新。

## [1.0.2] - 2026-08-21

### Changed

- 完成首轮功能导向命名整理，并统一仓库元数据与许可证名称。

## [1.0.1] - 2026-08-21

### Changed

- 根目录 `LICENSE` 替换为个人非商业许可证模板，并同步中文说明。
- 完成 README、Skill 元数据与 `agents/openai.yaml` 的一致性调整。

## [1.0.0] - 2026-08-20

### Added

- 首个完整 GitHub-ready Skill 版本。
- 固化“收到图片后自动生成四方案”的 MODE C 工作流。
- 固化 2–3 个背景内容区域的语义分区规则。
- 固化“同一完整主体 / 连续景物不得被机械拆分”的边界规则。
- 固化四方案结构：三套独立重新配色 + 一套原图配色。
- 固化所有方案必须存在显著大面积粗颗粒区。
- 固化主体轮廓、可选文本、事实字段、预设配色 / 文本 / 比例与质量控制规则。
