interface:
  display_name: "颗粒分域 · Grain Fields"
  short_description: "Scene-driven grain reconstruction for coherent image variants"
  default_prompt: "Use $grain-fields to reconstruct this image through a scene-driven grain field, preserve identity-critical content, and return visually validated coherent variants."
