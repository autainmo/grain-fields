---
name: grain-fields
description: "Use when a user provides an image and wants the established content-aware reconstruction workflow: analyze semantic background regions, preserve the main subject, generate four variants automatically, apply one large coarse-grain region in every variant, preserve user-supplied facts/text, and include one original-color variant plus three independently art-directed color variants."
---

# 颗粒分域 · Grain Fields

将用户图片按真实内容关系划分背景区域，在保持主体辨识度的前提下，用分区配色、整块大颗粒重构、主体轮廓线和适配文字一次生成 4 个完整方案。

## 1. 任务目标

- 对单张用户图片进行内容理解与视觉重构。
- 输出固定为 4 张完整方案图，而不是只做文字分析。
- 方案 1–3 使用彼此明显不同、且不受原图配色牵制的独立视觉方向。
- 方案 4 保留原图配色与构图，仅强化内容分区、主体轮廓和大颗粒区域。
- 四个方案的配色类型都必须不同；方案 1–3 不得彼此近似，且不得与方案 4 的原图配色保留方向混同。
- 四个方案的文本类型都必须不同，并分别结合各自配色、内容主题、主体、场景、构图和留白独立确定。
- 所有方案都必须存在一个面积大、颗粒单元大、第一眼可见的完整颗粒区域。
- 文本与附加信息仅按用户实际输入或可靠画面信息处理，不编造事实。

## 2. 适用条件

适用于用户上传或引用一张以以下任一对象为主要视觉主体的图片：

- 人物或群像；
- 动物、植物；
- 建筑、街道、室内空间；
- 山川、海岸、水面、自然地貌；
- 车辆、器物、产品、机械；
- 其他具有明确主体与环境关系的静态图片。

当用户要求“收到图直接执行”“自动生成多个重构方案”“大颗粒分区”“主体白色轮廓”“保留原图配色的一个方案”等目标时，应优先使用本 Skill。

## 3. 不适用条件

以下任务不应按本 Skill 默认流程执行：

- 只要求轻度修图、去瑕疵、调曝光或常规滤镜；
- 只要求裁剪、尺寸转换、背景抠图等单一机械操作；
- 用户明确要求完全不同的重构体系，且与本 Skill 的分区、大颗粒、四方案输出冲突；
- 用户明确要求只生成 1 张图或只分析不出图时，遵循用户当前明确要求。

## 4. 最高优先级规则

1. 用户当前明确要求优先于 Skill 默认值。
2. 用户提供的事实信息与原文文本优先于 AI 自动生成内容。
3. 主体身份、结构和关键辨识特征优先于风格化效果。除用户明确要求外，主体不要做明显重构，应尽量保持原图形，仅对影调和颜色参数做轻微优化微调。
4. 背景必须按内容与空间关系划分，不允许机械切割。
5. 所有默认方案都必须出现明显的大面积粗颗粒区域；在不破坏主体与场景关系的前提下，颗粒效果应尽可能明显。
5.1 核心颗粒背景区域在其他条件相近时，优先选择与主要主体直接相邻、紧挨主体的完整背景板块。若背景分区呈左中右、上中下或中心被外围包围的中间结构，则优先选择中间分区作为核心颗粒区。
5.2 核心颗粒效果必须在整个选定分区上进行重构，不得只做局部点缀或只覆盖选定分区的一小部分。
5.3 用户未明确要求其他描边方式时，主体周围严格默认使用中粗纯白色单线描边。
5.4 每一个重构结果都必须保持整体清晰；颗粒区允许概括细节，但不得造成整图模糊、主体发虚或低清感。
5.5 颗粒区与主体之间必须保留明确的视觉呼吸感，避免颗粒紧贴、吞没或压迫主体轮廓。
5.6 背景重构后的配色必须干净、协调、有秩序，不得出现杂乱、浑浊、脏灰或综合色失控的脏感。
6. 方案 4 的原图配色与原构图保留要求高于其他视觉处理。
7. 预设库只用于辅助选型；配色类型与文本类型可随机选取，但随机只发生在适配候选中，最终组合仍须保证画面效果好。

## 5. 用户输入识别

### 5.1 必需输入

- 至少一张可用于重构的图片。

### 5.2 可选文本与信息

以下字段全部可选，用户可填写任意一项、任意多项，也可全部删除或留空：

- 标题；
- 副标题；
- 正文；
- 地点；
- 时间；
- 相机信息；
- 镜头 / 焦段；
- 拍摄参数；
- 人物 / 主体信息；
- 事件 / 主题；
- 天气 / 环境；
- 摄影者 / 署名。

处理原则：

- 填写了什么，最终画面就合理展示什么；
- 未填写的事实信息不得编造；
- 用户原文可以调整字号、位置、断行和层级，但不得擅自改写其核心含义；
- 若用户未给文字，可根据每个视觉方案分别自动生成最适配的文字；四个方案的文本类型必须彼此不同；
- 若只给了部分事实信息，可使用已给信息，并补充不涉及事实造假的概念性或文学性文本。

## 6. 固定执行流程

收到用户图片与要求后，必须按以下顺序执行：

1. 读取 `references/00-style-core.md`，锁定风格不变量。
2. 按 `references/01-scene-analysis.md` 识别主体、背景内容层、文字安全区和候选颗粒区。
3. 按 `references/02-transformation.md` 决定哪些内容保留、转译、弱化或禁止修改。
4. 按 `references/03-art-direction.md` 确定 4 个视觉方案、分区配色、颗粒区域、轮廓线、文字和比例。
5. 按 `references/04-generation-compiler.md` 将每个方案编译为独立的图像生成指令。
6. 直接调用图像生成工具，生成 4 张图；不等待二次确认。
7. 按 `references/05-quality-control.md` 检查每张图。
8. 若不合格，只修正失败项并重新编译相关指令，再生成对应失败方案。
9. 四张均合格后输出。
10. 每完成并输出一组 4 张方案图后，提示用户：是否需要再生成一组；用户可直接回复“是”，也可同时指定配色、颗粒选区、文案和尺寸中的任意项。
11. 用户选择“是”后，不再要求二次确认，直接进入新一轮方案设计与图像生成；新一组 4 个方案必须与本轮此前已生成方案明显不同，同时继续保证每个方案内部的配色、颗粒选区、文案、尺寸和主体关系为当前约束下的最优组合。

流程图：

```text
收到用户图片与用户要求
        ↓
读取 00-style-core.md
        ↓
按 01-scene-analysis.md 理解原图
        ↓
按 02-transformation.md 决定内容取舍
        ↓
按 03-art-direction.md 确定最终视觉方案
        ↓
按 04-generation-compiler.md 编译图像生成指令
        ↓
直接调用图像生成
        ↓
按 05-quality-control.md 检查结果
        ↓
    ┌───────────┐
    │ 是否合格？ │
    └─────┬─────┘
       是 │ 否
       ↓  ↓
     输出  定点修正
       ↓        ↓
询问是否再生成一组  重新编译相关指令
       ↓        ↓
   否→结束     再生成
       ↓
   是→新一轮四方案
       ↓
  直接调用图像生成
```

## 7. 规则冲突优先级

发生冲突时按以下顺序处理：

1. 用户本轮明确命令；
2. 用户提供的文本、事实和禁止修改项；
3. `00-style-core.md` 的风格不变量；
4. `02-transformation.md` 的保留 / 转译边界；
5. `03-art-direction.md` 的具体视觉规则；
6. 预设配色、文本与比例库；
7. AI 自主判断。

只有上层规则未规定时，才允许使用下一层规则补足。

## 8. 默认执行行为

本 Skill 的默认模式为 **MODE C：一次自动生成多个方案**。

收到有效图片后：

1. 不只输出分析文字；
2. 不等待用户选择方案；
3. 不重复询问已经给出的信息；
4. 内部完成主体、背景、文字区、配色和颗粒区分析；
5. 直接生成 4 个方案；
6. 每个方案各 1 张，共 4 张；
7. 方案 1–3 重新设计配色；
8. 方案 4 保留原图配色与构图；
9. 所有方案都必须包含大面积粗颗粒区与主体轮廓线；
10. 每组四图完成后询问是否再生成一组；若用户选择“是”，立即生成下一组，不再重复确认。

若用户本轮明确要求改为先选方案再生成或只生成某一方案，则遵循用户本轮要求。

## 9. 用户文本与附加信息处理

- 用户填写的标题、副标题、正文：优先原样使用，按版面合理断行与分级。
- 用户填写的地点、时间、器材、人物身份等：视为事实字段，不得推断缺失值。
- 用户未填写文本：每个方案根据自身配色、主体、背景和故事性单独选取文本类型；四个方案的文本类型必须彼此不同，不得四张使用同一种文本形式。
- 用户只填写了打卡类事实：可将这些事实用于适合的方案；其他方案仍可自动生成内容型、文学型、档案型或其他适配文本，但不得伪造新的事实。
- 用户完全不需要文字时：若其明确要求无字，则四张均不加文字。

## 10. 多方案处理

默认四方案结构固定为：

- **方案 1：独立视觉方向 A**——重新配色，不受原图色彩限制；
- **方案 2：独立视觉方向 B**——与方案 1 在冷暖、明度、对比和气质上明显不同；
- **方案 3：独立视觉方向 C**——与前两者再次拉开视觉赛道；
- **方案 4：原图配色 + 大颗粒**——原配色、构图、主体位置和比例关系不变。

四个方案的配色类型都必须不同，文本类型也都必须不同。默认情况下，配色类型与文本类型应从各自预设库中在适配候选内随机抽选，并组合成当前画面下效果好的四套方案；不得机械重复，也不得为了随机而牺牲适配性。四个方案的图片尺寸默认均采用原图尺寸；只有用户明确指定尺寸，或本轮明确要求其他尺寸时，才允许改变。核心大颗粒背景区域仍需按方案独立判断，但优先选择紧挨主体的完整背景板块；若背景分区呈左中右、上中下或中心被外围包围的中间结构，则优先选择中间分区。颗粒效果必须覆盖整个选定分区，而不是只在分区内部做局部颗粒。

当用户要求“再生成一组”时，新一组不是上一组的轻微变体：4 个方案都要重新发散，并与本轮此前已经输出的方案在可调整维度上形成明显区别。用户可在再次生成时指定配色、颗粒选区、文案和尺寸中的任意一项或多项；**用户明确指定的项为硬约束，不得为了优化而擅自改写**，其余未指定项则围绕主体和这些硬约束重新寻找最优搭配。未指定任何附加条件时，由 AI 自动选择新的最优组合。方案 4 在用户未明确覆盖其规则时，仍遵守原图配色与原构图保留要求。

## 11. 输出规则

- 必须输出 4 张图，除非用户明确改变数量。
- 每张图必须是完整成图，不输出只含局部或色卡的替代物。
- 不需要先向用户展示内部分析过程。
- 生成失败或质检失败时，只重新生成失败方案，不必无条件重做全部四张。
- 若宿主工具支持多图批量生成，可批量执行；若不支持，则四个方案分别调用。
- 每组四图输出后必须附带一次简短询问：是否需要再生成一组；用户回复“是”即可直接触发下一组，也可在同一句中指定配色、颗粒选区、文案或尺寸。

## 12. 总体禁止行为

禁止：

- 将背景按简单上下、左右、几何比例机械切割；
- 把同一个完整主体或同一连续景物拆到多个背景区域；
- 将“大颗粒”弱化成细噪点、轻胶片颗粒或几乎不可见的纹理；
- 只在边角放一个很小的颗粒块来冒充核心颗粒区；
- 让颗粒覆盖主文字导致可读性下降；
- 方案 1–3 被原图配色牵制，导致三案几乎同色；
- 方案 4 改变原图主配色或构图；
- 擅自改变人物身份、建筑结构、动物种类、器物形态等核心特征；
- 擅自编造地点、时间、相机、镜头、人物身份、事件、天气、署名等事实；
- 为追求“高级感”“艺术感”而覆盖本 Skill 已规定的执行结构；
- 在多个文件中重新定义同一完整规则。
