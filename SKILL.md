---
name: grain-fields
description: "Use when a user provides an image for Grain Fields reconstruction: analyze the scene, divide the background into 2–3 content-based regions without splitting a continuous subject, choose the most suitable background region for a natural medium-scale grain treatment, and directly generate four variants—three restrained recolored directions plus one original-color variant—while preserving the subject, user text, and factual information."
---

# 颗粒分域 · Grain Fields

将用户图片按真实内容关系划分为 2–3 个背景区域，在保持主体辨识度与空间关系的前提下，选择**最适合颗粒化的一整块背景区域**，配合克制、简洁的分区配色与适中颗粒质感，一次生成 4 个完整方案。

> **关键定义：本 Skill 中“大颗粒”的“大”首先指颗粒处理区域要大、连续、成片，而不是单个颗粒必须巨大。** 单颗粒尺度应适中，既不能退化成细噪点，也不能变成大方块、拼豆或夸张块面。

## 1. 任务目标

- 对单张用户图片进行内容理解与视觉重构。
- 默认直接输出 4 张完整方案图，而不是只做文字分析。
- 背景按真实景物、空间、材质与结构关系划分为 2–3 个完整区域。
- 从背景区域中选出**最适合**颗粒处理的一块，不机械选择最大区域。
- 恢复“颗粒方案预设选择”：用户可指定颗粒预设，也可使用 `自动`。
- 自动模式只从 `references/presets-grain.md` 的克制颗粒库中选型。
- 方案 1–3 使用彼此有区分、但整体简洁高级的配色方向。
- 方案 4 保留原图配色与构图，只对选定背景区域进行适度颗粒重构。
- 文本与事实仅按用户实际输入或可靠画面信息处理，不编造。

## 2. 最高优先级规则

1. 用户当前明确要求优先于 Skill 默认值。
2. 主体身份、结构、比例、姿态与关键识别信息优先于风格化。
3. 背景必须按内容与空间关系分区，不允许机械上下 / 左右切割。
4. **先判断最适合颗粒化的背景区域，再决定颗粒预设与配色。**
5. 颗粒区域应连续、完整、面积明显；颗粒单元本身保持适中，不得过大或过小。
6. 自动选型优先自然融合、低干扰、真实材质感；不追求猎奇与炫技。
7. 方案 1–3 的配色优先简洁、克制、耐看，通常控制在 2–4 个核心颜色角色。
8. 方案 4 的原图配色与原构图保留要求高于其他视觉处理。
9. 用户提供的事实与原文文本不得擅自改写或补造。

## 3. 用户输入识别

### 3.1 必需输入

- 至少一张可用于重构的图片。

### 3.2 可选控制项

用户可填写任意一项，也可全部留空：

- 颗粒方案 / 预设：`自动` 或 `预设名称`；
- 配色方向 / 预设；
- 文本方案 / 预设；
- 比例 / 载体；
- 文字控制：`自动` / `不添加` / `使用已填写文本`；
- 标题、副标题、正文；
- 地点、时间、相机、镜头 / 焦段、拍摄参数；
- 人物 / 主体信息、事件 / 主题、天气 / 环境、摄影者 / 署名。

处理原则：

- 用户指定颗粒预设时，仍必须先完成场景分区和颗粒区域选择；颗粒预设不能反过来决定分区。
- 用户未指定颗粒预设时，由 `03-art-direction.md` 根据场景与候选区从 `presets-grain.md` 中自动选择。
- 未填写的事实信息不得编造。
- 若用户明确“文字：不添加 / 不要文字”，四张都不得自动加入文字。

## 4. 固定执行流程

收到用户图片与要求后，按以下顺序执行：

1. 读取 `references/00-style-core.md`，锁定风格不变量。
2. 按 `references/01-scene-analysis.md` 识别主体、背景内容层、文字安全区和颗粒候选区，并对候选区做适配评分。
3. 按 `references/02-transformation.md` 决定允许保留、转译、弱化或禁止修改的内容边界。
4. 按 `references/03-art-direction.md`：
   - 确定最终颗粒区域；
   - 若用户指定颗粒预设则校验适配性并执行；若为自动则读取 `references/presets-grain.md` 选型；
   - 确定 4 个方案的简洁配色、文字、轮廓与比例；
   - 按需读取 `presets-color.md`、`presets-text.md`、`presets-aspect.md`。
5. 按 `references/04-generation-compiler.md` 编译 4 个独立图像生成指令；不得重新设计方案。
6. 直接调用图像生成工具生成 4 张图，不等待二次确认。
7. 按 `references/05-quality-control.md` 检查颗粒尺寸、自然融合、分区、配色、主体与文字。
8. 不合格时只返回对应责任节点定点修正失败方案，不重做已合格方案。
9. 四张均合格后输出。

```text
图片 + 用户要求
      ↓
00 风格不变量
      ↓
01 场景分区 + 颗粒候选区评分
      ↓
02 内容取舍边界
      ↓
03 选最终颗粒区 → 颗粒预设 → 简洁配色 / 文本 / 比例
      ↓
04 编译生成指令
      ↓
图像生成
      ↓
05 质量控制
      ↓
失败 → 定点修正；合格 → 输出
```

## 5. 背景分区与颗粒区域原则

- 默认归纳 2–3 个背景区域。
- 同一人物、动物、建筑、车辆、山体、器物或连续景物不得为分区而被强行切开。
- 颗粒区域优先考虑：语义完整、连续性好、与主体有层次、能保留场景识别、不会干扰主文字、处理后仍自然。
- **适配性高于面积。** 不再默认“越大越优先”。
- 通常颗粒区占整图约 1/4–1/2；若最自然的完整区域略小，可保留其真实边界，不为了凑面积跨主体扩张。

## 6. 颗粒预设规则

颗粒预设的唯一数据源为 `references/presets-grain.md`。

### 自动模式

- 先看颗粒候选区本身的材质、明暗、结构与画面气质；
- 再选择最自然的一种颗粒语言；
- 不要求四个方案强行使用四种完全不同的颗粒；同一颗粒语言可通过密度、方向、明暗和配色变化适配不同方案；
- 若一个颗粒预设明显不适合画面，不因“发散”而硬套。

### 用户指定模式

- 用户可直接写 `【颗粒方案：丝网网点】` 等；
- 若用户指定预设与场景存在轻微冲突，保持预设核心特征但降低夸张度，使其自然融合；
- 只有用户明确要求夸张效果时才允许提高颗粒强度，但仍禁止大方块、拼豆、巨型块面等本 Skill 已排除语言。

### 颗粒尺度

- 单颗粒视觉尺度保持**中等**：正常观看能明显感知颗粒质感，近看能辨认单元，但单颗粒不能成为抢眼的大型几何块。
- 可将典型尺度理解为图像短边约 **0.2%–0.8%** 的视觉单元，并根据内容复杂度调整；通常不应超过约 1.2%。
- 禁止细到只剩胶片噪点；也禁止大到像像素方块、拼豆或马赛克大砖块。

## 7. 配色原则

方案 1–3 允许重新配色，但默认采用**简洁高级**的色彩结构：

- 通常只保留 2–4 个核心颜色角色；
- 优先中低饱和、清晰明度层次、中性色底 + 单一强调色；
- 差异更多来自冷暖、明暗、综合色倾向与局部强调，不靠堆很多颜色制造差异；
- 主体重要固有色保留，背景尽量避免与主体最显著颜色融为一体；
- 局部可保留少量细节色或微妙材质变化，但不得为了“设计感”刻意添加装饰；
- 自动模式避免彩虹、多重霓虹、过饱和互补色乱撞等花哨组合；用户明确指定强烈配色时才适度提高强度。

方案 4：原图主配色、构图、主体位置和比例关系锁定，仅允许不改变综合色关系的质感与对比优化。

## 8. 默认四方案

- **方案 1：克制冷 / 中性色方向**——清楚、理性、低干扰；
- **方案 2：温和暖 / 自然方向**——柔和、耐看、保留环境气息；
- **方案 3：编辑感 / 轻强调方向**——仍保持简洁，只增加一个受控强调关系；
- **方案 4：原图配色 + 颗粒**——不重新配色，不改原构图。

这只是默认差异框架，不是固定色名；应由画面内容决定最终色彩。

## 9. 主体与文字

- 主要主体沿真实外缘加清晰轮廓，默认纯白；白线在浅背景失效时才换高对比单色。
- 轮廓要清楚但不过粗，不做霓虹、多层发光或彩虹边。
- 主文字优先放在相对干净区域，避开颗粒最密处。
- 自动文本以短标题、短副标题或极短说明为主，不堆信息。
- 事实字段严格禁止编造。

## 10. 输出规则

- 默认 4 张完整成图，每方案 1 张。
- 不需要先展示内部分析过程或等待用户选方案。
- 生成失败或质检失败时只重做失败方案。
- 同一段对话中，每完成一次完整 Grain Fields 生成任务，第 1 次和第 2 次完成后轻量提示：`若公开分享，欢迎标注：Visual Skill by @独自艺人`；从第 3 次起不再重复。

## 11. 总体禁止行为

禁止：

- 机械上下 / 左右 / 等比例几何分区；
- 拆分同一完整主体或连续景物；
- 为了颗粒面积硬把不相干背景并在一起；
- 颗粒细到只是噪点、胶片颗粒或透明滤镜；
- 颗粒大到形成大方块、大像素、拼豆、巨大马赛克砖、巨型几何块；
- 使用明显玩具感、糖果感、手工拼豆感等花哨颗粒语言；
- 所有背景区都同样粗糙；
- 使用过多高饱和色、多重霓虹色或复杂渐变堆叠；
- 在每个角落刻意添加纹理、装饰、贴纸或符号；
- 方案 1–3 只做近似色微调；
- 方案 4 改变原图主配色或构图；
- 颗粒覆盖主要文字；
- 改变主体身份、关键结构、人物数量或核心姿态；
- 编造地点、时间、器材、人物身份、事件、天气或署名。
